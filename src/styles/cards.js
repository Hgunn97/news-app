import {colours, dimensions} from '../styles/base.js';

export const button = {
  borderRadius: 0,
  marginLeft: 0,
  marginRight: 0,
  marginBottom: 0,
  backgroundColor: colours.success,
};

export const sizing = {
  height: dimensions.fullHeight,
};
